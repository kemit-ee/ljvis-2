# Copy this file to .env and fill in real values.
# .env is gitignored — never commit secrets.

# ── XTR / X-Road ──────────────────────────────────────────────────────────────
# Ruuter reads credentials from constants.ini (not from this file).
# To use a local override: copy constants.ini → constants.local.ini (gitignored),
# fill in real values, then uncomment CONSTANTS_FILE below.
# CONSTANTS_FILE=./constants.local.ini
XTR_KEYSTORE_PASSWORD=defaultpassword
XROAD_SECURITY_SERVER=
XROAD_INSTANCE=ee-test

# ── Cloudflare R2 / S3-compatible storage ─────────────────────────────────────
# Endpoint WITHOUT trailing slash, WITHOUT bucket name
S3_ENDPOINT=https://c3392559e79453c4b1ad8cffbec2b160.r2.cloudflarestorage.com
S3_BUCKET_NAME=ljvis
S3_REGION=auto
S3_ACCESS_KEY_ID=your-r2-access-key-id-here
S3_SECRET_ACCESS_KEY=your-r2-secret-access-key-here

# ── s3-proxy upload validation (all optional — defaults shown) ─────────────────
# S3 key prefix (folder) for all uploads
S3_KEY_PREFIX=protokollid
# Maximum allowed file size in megabytes
S3_MAX_SIZE_MB=20
# Maximum sanitized filename length (stem only, before extension)
S3_MAX_FILENAME_LENGTH=200
# Comma-separated list of allowed MIME types (leave empty to allow all detected types)
S3_ALLOWED_MIME_TYPES=application/pdf,image/jpeg,image/png,application/vnd.etsi.asic-e+zip,application/msword,application/vnd.openxmlformats-officedocument.wordprocessingml.document

# ── Postkast 2.0 (LJVIS-2 teavituste moodul — UC-01/UC-04) ────────────────────
# RIA väljastatavad mandaadid — ootel. Täida siis, kui RIA annab.
# URL: PK 2.0 notification-management API baasaadress (ilma /notification-management/v1/ lõputa)
PK_URL=https://postkast2-api.example.ee
# Bearer token PK 2.0 autentimiseks
PK_TOKEN=your-pk2-bearer-token-here
# Mallide ID-d (vastavad PK 2.0 template/v1/templates-is registreeritud mallidele)
# Malli loomise juhend: docs/pk2-templates/README.md
PK_TEMPLATE_CARRIER_VIOLATION=
PK_TEMPLATE_LABOR_KABOTAGE=
PK_TEMPLATE_LABOR_FOREIGN_PROPOSAL=
# Tööinspektsiooni e-post (labor_kabotage ja labor_foreign_proposal teavituste saaja)
TI_EMAIL=
# LJVIS_RUUTER_INTERNAL — Ruuter.internal baasaadress create.yml ws_send jaoks
# Vaikimisi: http://ruuter-internal:8089 (docker-compose teenuse nimi)
LJVIS_RUUTER_INTERNAL=http://ruuter-internal:8089
