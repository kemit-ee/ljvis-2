export interface ControlForm {
  name: string;
  route: string;
  hasParent: boolean;
}

export interface FormClassifierValue {
  name: string;
  description: string;
  hasParent: boolean;
}
