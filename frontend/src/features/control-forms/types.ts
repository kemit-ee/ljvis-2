export interface ControlForm {
  name: string;
  route: string;
  hasParent: boolean;
}

export interface BatchFormClassifierValue {
  name: string;
  description: string;
  hasParent: boolean;
  classifierCode: string;
}
