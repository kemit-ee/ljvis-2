# NotifyCheckResult_Request — sends an NCR outbound notification to the ERRU MOVEHUB.
#
# Estonia notifies the transport undertaking's home MS of a check result.
# This is a one-shot notification with a synchronous hub acknowledgement.
#
# technicalId/workflowId are generated by Ruuter (send.yml) BEFORE this call and stored
# for correlation — see ctud-request.yml comment for the same rationale.
#
# Note: SeriousInfringement / MinorInfringement sub-elements are not yet supported here —
# the current NCR send flow only passes summary-level data (checkResult). Add these when
# the full infringement detail form is implemented (future task).
#
# Endpoint URLs:
#   Acceptance: https://webgate.acceptance.ec.testa.eu/move-hub/ERRU/soap/ReqRes/ERRU.svc
#   Production:  https://webgate.ec.testa.eu/move-hub/ERRU/soap/ReqRes/ERRU.svc

params:
  - ncrTo
  - businessCaseId
  - technicalId
  - workflowId
  - originatingAuthority
  - requestSource
  - requestPurpose
  - transportUndertakingName
  - communityLicenceNumber
  - vehicleRegistrationNumber
  - vehicleRegistrationCountry
  - checkResult
  - sentAt

method: POST

service: https://webgate.acceptance.ec.testa.eu/move-hub/ERRU/soap/ReqRes/ERRU.svc

soap_action: NotifyCheckResult_Request

envelope: >
  <?xml version="1.0" encoding="UTF-8"?>
  <soapenv:Envelope xmlns:soapenv="http://schemas.xmlsoap.org/soap/envelope/"
                    xmlns:erru="https://webgate.ec.testa.eu/move-hub/erru/3.5">
    <soapenv:Header/>
    <soapenv:Body>
      <erru:NotifyCheckResult_Request>
        <erru:Header version="3.5"
                     technicalId="{{#if technicalId}}{{technicalId}}{{else}}{{generate.uuid}}{{/if}}"
                     workflowId="{{#if workflowId}}{{workflowId}}{{else}}{{generate.uuid}}{{/if}}"
                     sentAt="{{sentAt}}"
                     from="EE"
                     to="{{ncrTo}}"/>
        <erru:Body businessCaseId="{{businessCaseId}}"
                   originatingAuthority="{{originatingAuthority}}"
                   requestSource="{{requestSource}}"
                   requestPurpose="{{requestPurpose}}">
          <erru:TransportUndertaking transportUndertakingName="{{transportUndertakingName}}"
                                     communityLicenceNumber="{{communityLicenceNumber}}">
            <erru:Vehicle vehicleRegistrationNumber="{{vehicleRegistrationNumber}}"
                          vehicleRegistrationCountry="{{vehicleRegistrationCountry}}"/>
            <erru:CheckSummary checkResult="{{checkResult}}"/>
          </erru:TransportUndertaking>
        </erru:Body>
      </erru:NotifyCheckResult_Request>
    </soapenv:Body>
  </soapenv:Envelope>
