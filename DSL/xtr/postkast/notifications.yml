# Postkast 2.0 notification-management/v1/notifications — REST passthrough.
#
# `kind: rest` (XTR >= 0.3.0-rc): verbatim byte passthrough to the X-Road
# Security Server. XTR sets `X-Road-Client` itself from xtr.yaml `client_data`
# and forwards every other inbound header (X-EXTERNAL-ID) and the body unchanged.
#
# Endpoint: POST /postkast/notifications
#   (called via [#PK_NOTIFICATIONS_ENDPOINT] from
#    DSL/Ruuter.internal/ljvis/POST/notification/send-postkast.yml)
# URL built by XTR:
#   {security_server.url}/r1/{xroad_instance}/GOV/70006317/postkast/notification-management/v1/notifications
kind: rest
method: POST

target:
  member_class: GOV
  member_code: "70006317"
  subsystem_code: postkast
  service_code: notification-management
  path: /v1/notifications

allowed_query_params: []
forward_body: true
