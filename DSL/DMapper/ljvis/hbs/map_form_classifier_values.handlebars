[
    {{#each values}}
    {
        "classifierCode": "{{this.classifierCode}}",
        "name": "{{this.name}}",
        "description": "{{this.description}}",
        "hasParent": {{this.hasParent}}
    }{{#unless @last}},{{/unless}}
    {{/each}}
]