{{#each classifierValues}}{{#if @first}}
{
    "classifierId": "{{this.classifierId}}",
    "classifierValueId": "{{this.classifierValueId}}",
    "code": "{{this.code}}",
    "name": "{{this.name}}",
    "validFrom": "{{this.validFrom}}",
    "validUntil": {{#if this.validUntil}}"{{this.validUntil}}"{{else}}null{{/if}}
}
{{/if}}{{/each}}