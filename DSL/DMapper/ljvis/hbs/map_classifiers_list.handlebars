{
    "content": [
        {{#each classifiers}}
        {
            "id": "{{this.id}}",
            "code": "{{this.code}}",
            "name": "{{this.name}}",
            "description": "{{this.description}}"
        }{{#unless @last}},{{/unless}}
        {{/each}}
    ],
    "total": {{#with classifiers.[0]}}{{total}}{{else}}0{{/with}}
}
