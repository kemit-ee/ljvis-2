[
    {{#each values}}
    {
        "name": "{{this.name}}",
        "description": "{{this.description}}",
        "hasParent": {{this.hasParent}}
    }{{#unless @last}},{{/unless}}
    {{/each}}
]