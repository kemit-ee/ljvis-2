# mock_list.yml — klassifikaatori väärtuste nimekirja mock

call_mock:
  call: http.post
  args:
    url: "[#LOCAL_RESQL]/dev/iam/classifier_value/mock_list"
  result: mock_response
  next: respond

respond:
  wrapper: false
  return: ${mock_response.response.body}
  next: end
