# mock_get.yml — klassifikaatori detaili mock

call_mock:
  call: http.post
  args:
    url: "[#LOCAL_RESQL]/dev/iam/classifier/mock_get"
  result: mock_response
  next: respond

respond:
  wrapper: false
  return: ${mock_response.response.body}
  next: end
