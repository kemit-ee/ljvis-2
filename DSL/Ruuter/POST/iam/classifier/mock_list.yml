# mock_list.yml — klassifikaatorite nimekirja mock

call_mock:
  call: http.post
  args:
    url: "[#LOCAL_RESQL]/dev/iam/classifier/mock_list"
  result: mock_response
  next: respond

respond:
  wrapper: false
  return: ${mock_response.response.body}
  next: end
