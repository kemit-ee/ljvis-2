# mock_check_code_exists.yml — väärtuse koodi kontrolli mock

call_mock:
  call: http.post
  args:
    url: "[#LOCAL_RESQL]/dev/iam/classifier_value/mock_check_code_exists"
  result: mock_response
  next: respond

respond:
  wrapper: false
  return: ${mock_response.response.body}
  next: end
