# mock_update.yml — väärtuse uuenduse mock

call_mock:
  call: http.post
  args:
    url: "[#LOCAL_RESQL]/dev/v1/iam/classifier_value/mock_update"
  result: mock_response
  next: respond

respond:
  wrapper: false
  return: ${mock_response.response.body}
  next: end
