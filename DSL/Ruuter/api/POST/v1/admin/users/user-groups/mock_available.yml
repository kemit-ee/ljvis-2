get_available:
  call: http://[#LOCAL_RESQL]/ljvis2/iam/user_group_membership/v1/mock_available
  args:
    userId:         "${incoming.body.userId}"
    organisationId: "${incoming.body.organisationId}"
  result: db_response
return: "${db_response.response.body}"
