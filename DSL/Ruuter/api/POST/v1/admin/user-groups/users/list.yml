get_users:
  call: http://[#LOCAL_RESQL]/ljvis2/iam/user_group/v1/list_users
  args:
    userGroupId: "${incoming.body.userGroupId}"
    page:        "${incoming.body.page}"
    pageSize:    "${incoming.body.pageSize}"
  result: db_response
return: "${db_response.response.body}"
