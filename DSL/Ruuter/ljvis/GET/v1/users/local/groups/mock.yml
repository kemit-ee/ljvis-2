mock_step:
  call: reflect.mock
  args:
    response:
      items:
        - userGroupId: "2"
          name: "Local Admin Group"
  result: mock_data

returnSuccess:
  return: ${mock_data.response.body.items}
  next: end
