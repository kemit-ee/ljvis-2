declaration:

  version: "1.0"
  description: "Get version history snapshots for a TRAM control card driver sub-form. Guarded by tram_driver_form.read via the tram-form/.guard."
  namespace: control-forms
  allowlist:
    params:
      - field: id
        type: string
        description: "sp_driver_form_key"

extractRequestData:
  assign:
    id: "${incoming.params.id}"
  next: getSnapshots

getSnapshots:
  call: http.post
  args:
    url: "[#LJVIS_RESQL]/control-forms/tram-form/sp-driver/get-snapshots"
    headers:
      type: json
    body:
      id: ${id}
  result: res
  next: returnSuccess

returnSuccess:
  return: ${res.response.body}
  next: end
