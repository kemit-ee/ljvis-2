declaration:
  version: "1.0"
  description: >-
    Postkast 2.0 kaudu saadetud kirjade logi (UC-02).
    Filtrid: status, messageType, dateFrom.
    Leheküljed: page + pageSize.
    Nõuab notification.admin õigust (kaitstud outbound-log/.guard kaudu).
  namespace: notification
  allowlist:
    params:
      - field: status
        type: string
      - field: messageType
        type: string
      - field: dateFrom
        type: string
      - field: page
        type: number
      - field: pageSize
        type: number

extractParams:
  assign:
    status: "${incoming.params.status ?? ''}"
    message_type: "${incoming.params.messageType ?? ''}"
    date_from: "${incoming.params.dateFrom ?? ''}"
    page: "${incoming.params.page ?? 1}"
    page_size: "${incoming.params.pageSize ?? 20}"
  next: queryLog

queryLog:
  call: http.post
  args:
    url: "[#LJVIS_RESQL]/notification/list_outbound_log"
    headers:
      type: json
    body:
      status: ${status}
      message_type: ${message_type}
      date_from: ${date_from}
      page: ${page}
      page_size: ${page_size}
  result: res
  next: mapResponse

# Mähime read {content, total} kujusse (frontend PagedResponse), sama mustriga
# nagu erru list-otspunktid (map_rsi_list / map_ctud_list). total tuleb
# list_outbound_log.sql-i COUNT(*) OVER () veerust, mis on igal real ühesugune.
mapResponse:
  call: http.post
  args:
    url: "[#LJVIS_DMAPPER_HBS]/map_outbound_log_list"
    headers:
      type: json
    body:
      rows: ${res.response.body}
  result: mapped
  next: returnResult

returnResult:
  return: ${mapped.response.body}
  next: end
