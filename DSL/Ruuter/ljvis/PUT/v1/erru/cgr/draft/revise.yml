declaration:
  version: "1.0"
  description: "Revise an existing outgoing CGR request draft (LJVIS2-138, 'täiendamine'). PUT with the id in the request body, mirroring PUT /v1/erru/ctud. Appends a new snapshot with version + 1; nothing is ever updated in place. Editability is enforced in append-draft-revision.sql, which yields zero rows unless the latest snapshot is status='initiated' AND direction='outgoing' — so a sent request and an inbound request are both rejected with 422 not_editable regardless of the caller's permissions."
  method: put
  accepts: json
  returns: json
  namespace: erru
  allowlist:
    body:
      - field: id
        type: string
      - field: cgrTo
        type: string
      - field: originatingAuthority
        type: string
      - field: requestSource
        type: string
      - field: requestPurpose
        type: string
      - field: tmFirstName
        type: string
      - field: tmFamilyName
        type: string
      - field: tmDateOfBirth
        type: string
      - field: tmPlaceOfBirth
        type: string
      - field: certificateNumber
        type: string
      - field: certificateIssueDate
        type: string
      - field: certificateIssueCountry
        type: string

getActorFromTim:
  call: http.get
  args:
    url: "[#LJVIS_TIM]/jwt/userinfo"
    headers:
      cookie: ${incoming.headers.cookie}
  result: timRes
  next: extractRequestData

extractRequestData:
  assign:
    id: "${(incoming.body.id || '').trim()}"
    cgr_to: "${(incoming.body.cgrTo || '').trim().toUpperCase()}"
    originating_authority: "${(incoming.body.originatingAuthority || '').trim().toUpperCase()}"
    request_source: "${(incoming.body.requestSource || '').trim()}"
    request_purpose: "${(incoming.body.requestPurpose || '').trim()}"
    tm_first_name: "${(incoming.body.tmFirstName || '').trim().toUpperCase()}"
    tm_family_name: "${(incoming.body.tmFamilyName || '').trim().toUpperCase()}"
    tm_date_of_birth: "${(incoming.body.tmDateOfBirth || '').trim()}"
    tm_place_of_birth: "${(incoming.body.tmPlaceOfBirth || '').trim().toUpperCase()}"
    certificate_number: "${(incoming.body.certificateNumber || '').trim().toUpperCase()}"
    certificate_issue_date: "${(incoming.body.certificateIssueDate || '').trim()}"
    certificate_issue_country: "${(incoming.body.certificateIssueCountry || '').trim().toUpperCase()}"
    actor_personal_code: "${timRes.response.body.personalCode || ''}"
    actor_name: "${(timRes.response.body.firstName || '') + ' ' + (timRes.response.body.lastName || '')}"
  next: checkIdPresent

checkIdPresent:
  switch:
    - condition: ${id === ''}
      next: returnIdRequired
  next: validateRequest

validateRequest:
  template: "templates/erru/cgr/validate-cgr-request"
  requestType: GET
  body:
    cgrTo: ${cgr_to}
    originatingAuthority: ${originating_authority}
    requestSource: ${request_source}
    requestPurpose: ${request_purpose}
    tmFirstName: ${tm_first_name}
    tmFamilyName: ${tm_family_name}
    tmDateOfBirth: ${tm_date_of_birth}
    tmPlaceOfBirth: ${tm_place_of_birth}
    certificateNumber: ${certificate_number}
    certificateIssueDate: ${certificate_issue_date}
    certificateIssueCountry: ${certificate_issue_country}
  result: validationResult
  next: checkValidationResult

checkValidationResult:
  switch:
    - condition: ${validationResult.indexOf('"valid":true') === -1}
      next: returnValidationError
  next: reviseDraft

reviseDraft:
  call: http.post
  args:
    url: "[#LJVIS_RESQL]/erru/cgr/append-draft-revision"
    headers:
      type: json
    body:
      key: ${id}
      cgrTo: ${cgr_to}
      originatingAuthority: ${originating_authority}
      requestSource: ${request_source}
      requestPurpose: ${request_purpose}
      tmFirstName: ${tm_first_name}
      tmFamilyName: ${tm_family_name}
      tmDateOfBirth: ${tm_date_of_birth}
      tmPlaceOfBirth: ${tm_place_of_birth}
      tmFirstNameSearchKey: ""
      tmFamilyNameSearchKey: ""
      certificateNumber: ${certificate_number}
      certificateIssueDate: ${certificate_issue_date}
      certificateIssueCountry: ${certificate_issue_country}
      handlerPersonalCode: ${actor_personal_code}
      handlerName: ${actor_name}
      created_by: ${actor_personal_code}
  result: res
  next: checkReviseResult

# Zero rows means the SQL guard rejected the revision: the request is not an
# editable outgoing draft (already sent, or inbound), or the key does not exist.
checkReviseResult:
  switch:
    - condition: ${res.response.body == null || res.response.body.length != 1}
      next: returnNotEditable
  next: extractReviseResult

extractReviseResult:
  assign:
    business_case_id: "${res.response.body[0].businessCaseId}"
    new_version: "${res.response.body[0].version}"
    new_status: "${res.response.body[0].status}"
  next: logAuditEvent

logAuditEvent:
  template: "templates/audit/erru/cgr/log-cgr-request-save"
  requestType: GET
  headers:
    cookie: ${incoming.headers.cookie}
  body:
    business_case_id: ${business_case_id}
    source_business_case_id: ""
    changed_fields: "päringu väljad"
    status: ${new_status}
    version: ${new_version}
    actor_name: ${actor_name}
    actor_personal_code: ${actor_personal_code}
  result: audit_result
  next: returnSuccess

returnSuccess:
  return: ${res.response.body[0]}
  next: end

returnValidationError:
  status: 422
  return: ${validationResult}
  next: end

returnIdRequired:
  status: 422
  return: '{"type":"VALIDATION_ERROR","field":"id","code":"required"}'
  next: end

returnNotEditable:
  status: 422
  return: '{"type":"VALIDATION_ERROR","field":"status","code":"not_editable"}'
  next: end
