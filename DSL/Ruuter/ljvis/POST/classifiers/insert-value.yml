declaration:
  call: declare
  version: 0.1
  description: "Create a new classifier value"
  method: post
  accepts: json
  returns: json
  namespace: classifiers
  allowlist:
    body:
      - field: classifierId
        type: string
      - field: code
        type: string
      - field: name
        type: string
      - field: validFrom
        type: string
      - field: validUntil
        type: string

authenticate:
  template: "[#LJVIS_PROJECT_LAYER]/check-user-authority"
  requestType: templates
  headers:
    cookie: ${incoming.headers.cookie}
  result: auth_user
  next: check_permission

check_permission:
  switch:
    - condition: ${auth_user.permissions != null && auth_user.permissions.indexOf('classifier_value.edit') >= 0}
      next: extractRequestData
  next: return_forbidden

extractRequestData:
  assign:
    classifier_id: "${incoming.body.classifierId}"
    code: "${incoming.body.code}"
    name: "${incoming.body.name}"
    valid_from: "${incoming.body.validFrom}"
    valid_until: "${incoming.body.validUntil ?? ''}"
    created_by: "${auth_user.id}"
  next: insertClassifierValue

insertClassifierValue:
  call: http.post
  args:
    url: "[#LJVIS_RESQL]/classifier/insert_classifier_value"
    headers:
      type: json
    body:
      classifier_id: ${classifier_id}
      code: ${code}
      name: ${name}
      created_by: ${created_by}
  result: insertRes
  next: createClassifierValueValidityState

createClassifierValueValidityState:
  call: http.post
  args:
    url: "[#LJVIS_RESQL]/classifier/insert_classifier_value_validity_state"
    headers:
      type: json
    body:
      classifier_value_id: ${insertRes.response.body[0].id}
      valid_from: ${valid_from}
      valid_until: ${valid_until}
      created_by: ${created_by}
  result: validityStateRes
  next: rebuildLatest

rebuildLatest:
  call: http.post
  args:
    url: "[#LJVIS_RESQL]/classifier/rebuild_classifier_value_latest"
    headers:
      type: json
    body:
      classifier_id: ${classifier_id}
      created_by: ${created_by}
  result: latestRes
  next: returnSuccess

returnSuccess:
  return: ${validityStateRes.response.body}
  next: end

return_forbidden:
  status: 403
  return: "insufficient permissions"
  next: end