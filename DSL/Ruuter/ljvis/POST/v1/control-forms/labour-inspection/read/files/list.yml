declaration:

  version: "1.0"
  description: "List active file attachments for a labour inspection form"
  namespace: control-forms
  allowlist:
    body:
      - field: formNumber
        type: string

getFiles:
  template: "templates/files/list"
  requestType: GET
  headers:
    cookie: ${incoming.headers.cookie}
  body:
    form_number: ${incoming.body.formNumber}
  result: listResult
  next: returnSuccess

returnSuccess:
  return: ${listResult}
  status: 200
  next: end
