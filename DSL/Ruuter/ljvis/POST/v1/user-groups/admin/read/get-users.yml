declaration:
  call: declare
  version: 0.1
  description: "List members of a user group (admin scope — all organisations)"
  method: post
  accepts: json
  returns: json
  namespace: user-groups
  allowlist:
    body:
      - field: page
        type: number
        description: "Page number"
      - field: pageSize
        type: number
        description: "Items per page"
      - field: sorting
        type: string
        description: "Sort column and direction"
      - field: search
        type: string
        description: "Search by first or last name"
      - field: userGroupId
        type: string
        description: "Filter by user group"

extractRequestData:
  assign:
    page: "${incoming.body.page ?? 1}"
    page_size: "${incoming.body.pageSize ?? 20}"
    sorting: "${incoming.body.sorting ?? 'status asc'}"
    search: "${incoming.body.search ?? ''}"
    user_group_id: "${incoming.body.userGroupId}"
    user_organisation_id: ""
  next: getUsers

getUsers:
  call: http.post
  args:
    url: "[#LJVIS_RESQL]/user_group/get_user_group_users"
    headers:
      type: json
    body:
      page: ${page}
      page_size: ${page_size}
      sorting: ${sorting}
      search: ${search}
      user_group_id: ${user_group_id}
      user_organisation_id: ${user_organisation_id}
  result: res

mapResponse:
  call: http.post
  args:
    url: "[#LJVIS_DMAPPER_HBS]/map_user_group_users"
    headers:
      type: json
    body:
      users: ${res.response.body}
  result: mapped

returnSuccess:
  return: ${mapped.response.body}
  next: end
