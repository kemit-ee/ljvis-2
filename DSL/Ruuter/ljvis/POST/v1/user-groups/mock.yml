mock_step:
  call: reflect.mock
  args:
    response:
      items:
        - id: "99"
          name: "New Group"
  result: mock_data

returnSuccess:
  return: ${mock_data.response.body.items}
  next: end
