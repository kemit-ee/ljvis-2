# Runs at 01:00 Tallinn time (TZ pinned in docker-compose.yml).
etoimik_decision_sync:
  trigger: "0 0 1 * * ?"
  type: exec
  command: curl -sf --retry 2 --retry-delay 60 -X POST "${RUUTER_INTERNAL}/ljvis/cron/etoimik-decision-sync" || true
  allowedEnvs:
    - RUUTER_INTERNAL
