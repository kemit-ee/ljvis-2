# LJVIS2-12: nightly deactivation of users whose access_end has passed.
# Logic lives in DSL/Ruuter.internal/ljvis/POST/users/deactivate-expired.yml
# + DSL/Resql/ljvis/POST/user/deactivate_expired_users.sql — this file only
# schedules the call.
#
# Trigger is Quartz-cron (sec min hour day month day-of-week), evaluated in
# the container's local time. Set TZ=Europe/Tallinn on the cronmanager
# service if it needs to be exactly 02:00 Tallinn time.
deactivate_expired_users:
  trigger: "0 0 2 * * ?"
  type: http
  method: POST
  url: http://ruuter-internal:8080/ljvis/users/deactivate-expired
  retryCount: 2
  retryDelay: 60000
  ignoreFailures: true
