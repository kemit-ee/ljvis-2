# LJVIS2-135/58/23: hourly sync of extraordinary technical inspection
# status. Logic lives in DSL/Ruuter.internal/ljvis/POST/cron/yvkehtivus-sync.yml
# (candidate selection, X-Road query, in-place field write, audit).
#
# Runs at :05 every hour (offset from :00 to avoid contending with other
# jobs). Tallinn time (TZ pinned in docker-compose.yml).
yvkehtivus_sync:
  trigger: "0 5 * * * ?"
  type: exec
  command: curl -sf --retry 2 --retry-delay 60 -X POST "${RUUTER_INTERNAL}/ljvis/cron/yvkehtivus-sync" || true
  allowedEnvs:
    - RUUTER_INTERNAL
